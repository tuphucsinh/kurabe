# HANDOFF — Kurabe QAQC

## Trạng thái hiện tại
- Phase 95 `True static-first` đã hoàn tất; Phase 96 đang triển khai theo integrity-first WBS.
- P96T00 live preflight PASS: Active = 1, evaluations = 53, rounds = 69, duplicate/orphan = 0; catalog metadata vẫn UNKNOWN.
- P96T01 baseline PASS bằng Mika Playwright fallback: 3 cold + 3 warm mỗi viewport; median first-light→full 390 `3313/2685ms`, 768 `2898/2480ms`, 1440 `2994/2786ms`.
- P96T02 PASS local candidate: RSC/server-only Active resolver, exact periodId query/cache, fail-closed zero/multiple/error, Closed-only inline history; 28/28 tests + lint/tsc/build/browser canary PASS.
- P96T03 PASS_WITH_CONSTRAINT: candidate partial unique index + preflight/rollback; close/target Active + exactly-one affected-row guards; create DB uniqueness authority; 29/29 tests + lint/tsc/build + fresh reviewer PASS.
- Live catalog P96T03 `UNKNOWN/BLOCKED`: Management API database query và `supabase db query --linked` đều 403. Không claim live index verified.
- P96T03 candidate chưa apply; production migration/apply cần direct catalog privilege và approval riêng. Chưa deploy/push.
- P96T04 PASS_WITH_CONSTRAINT: atomic create RPC giữ evaluator resolution ở TypeScript; exact-empty Closed delete RPC lock/count/rowcount guard, không delete child business rows; focused contract + full suite 30/30 + lint/tsc/build PASS, fresh `gemini-3.1-pro-high` review PASS clean/HIGH.
- P96T04 live DB integration/failure injection và catalog verification vẫn `UNKNOWN/BLOCKED`; migration chưa apply. P96T05 candidate đã hoàn tất local nhưng live catalog/race/failure injection vẫn `UNKNOWN/BLOCKED`; Phase 97 vẫn DEFERRED.
- P96T05 PASS_WITH_CONSTRAINT: shared server-only exact-Active guard phủ evaluation/AI writes + lazy initializer; new wrapper RPC locks period `FOR UPDATE` and delegates P3 transaction; multiple Active fail-closed. Focused contract + full suite 31/31 + lint/tsc/build PASS; fresh reviewer PASS, Critical/Important NONE. Residual: direct REST TOCTOU, lazy N+1 guard. No migration/apply/deploy/push.
- P96T06 PASS_WITH_CONSTRAINT: compare static/loading frame + stable layer markers; reusable header/frame wraps loaded/error/blocked states, preserving back navigation and private-data safety. Focused + full suite 32/32 + lint/tsc/build PASS; fresh reviewer R2 PASS, Critical/Important NONE. Browser authenticated canary BLOCKED_AUTH (307 `/login`); no visual PASS claim.
- P96T07 PASS_WITH_CONSTRAINT: aggregate compare source unchanged; truthful primary/secondary phase markers and DOM order added, without fake streaming/delay/retry/query. Focused + full suite 33/33 + lint/tsc/build PASS; fresh reviewer PASS, Critical/Important NONE. Browser authenticated canary BLOCKED_AUTH (307 `/login`); no visual PASS claim.
- P96T08 PASS_WITH_CONSTRAINT: visible criterion IDs removed from changed/unchanged output, shared memoized `comparisonRows` feeds mobile/desktop, unchanged uses native closed-by-default `<details>/<summary>`, modest density reduction. Focused + full suite 34/34 + lint zero warnings + tsc/build PASS; fresh reviewer R2 PASS, Critical/Important NONE. Browser authenticated canary BLOCKED_AUTH (307 `/login`); no visual PASS claim.
- P96T09 PARTIAL/UNKNOWN: source commit `4ff47f3` clean; full 34/34 + lint/typecheck/build PASS; authenticated compare canary PASS ở desktop CSS viewport `1280x633` — browser_vision top-of-page rendered snapshot, live identity/score, overflow delta `0`, native disclosure keyboard open/close, detail→compare→back, hard reload và console/JS errors `0`; local navigation samples `1075.6/581.6/502.7 ms`, median `581.6 ms`. Browser `window.resizeTo()` không đổi viewport nên mobile/tablet/1440 authenticated screenshots chưa có evidence độc lập hợp lệ; cross-tab stale/cache transition và live Active→Closed/none→new Active vẫn UNKNOWN. Branch cost `$0.01344/hour` nên không tạo theo approval chỉ `$0`; không phát sinh charge. Disposable local PostgreSQL compatibility harness apply nguyên văn P96T03–P96T05 candidates và PASS index/RPC/rollback/concurrency tests (1 commit + 1 duplicate-key, final active count 1), nhưng đây không phải production parity. Supabase MCP read-only catalog đã xác nhận live index, P96T04 RPCs và P96T03–P96T05 migration rows đều absent. Chưa có final Phase 96 PASS, không apply/deploy/push.
- P96T09 PASS_WITH_CONSTRAINT: source commit `4ff47f3` clean; full 34/34 + lint/typecheck/build PASS; authenticated compare canary PASS ở desktop CSS viewport `1280x633` — browser_vision top-of-page rendered snapshot, live identity/score, overflow delta `0`, native disclosure keyboard open/close, detail→compare→back, hard reload và console/JS errors `0`; local navigation samples `1075.6/581.6/502.7 ms`, median `581.6 ms`. Chrome thật/CDP authenticated matrix đã capture top-of-page screenshots hợp lệ tại `/home/pi5/hermes-artifacts/browser-evidence/kurabe/p96t09-cdp-final/{390x844,768x1024,1440x900}.png`; cả 3 viewport overflow `0`, live data, markers và unchanged disclosure closed-by-default PASS. CDP reload có `0` JS exception; chỉ CSP report-only messages đã biết. Two-tab cùng profile render live data; reload transient rồi ready; shared cache/invalidation và lifecycle Active→Closed/none→new Active chưa chứng minh. Geometry thật cho thấy global ChatWidget overlap comments card cả 3 viewport; giữ là pre-existing global UI finding, không sửa trong P96 để bảo toàn chat behavior. Branch cost `$0.01344/hour` nên không tạo theo approval `$0`; không phát sinh charge. Production đã apply exact P96T03→P96T04→P96T05 một lần qua MCP `apply_migration`; ledger versions `20260826140550`, `20260826140552`, `20260826140554`; postflight index/RPC/provenance/permissions PASS, counts `1/1/53/69`, duplicate/orphan `0`. Live safe transaction probes PASS và no-residue: Active create collision rollback, Closed+HAS_DATA delete guard, Closed-write firewall. True 1-winner production race chưa chạy vì cần đóng Active hiện hữu/no-active window; disposable local race PASS `1 commit + 1 duplicate-key`, final active `1`. Application artifact deployed production qua Vercel `dpl_FyWPJ9HdXL6HdVTJsjzaBMa3TtRp`, `READY`, alias `https://lykiv.vercel.app`; `/login` HTTP 200/browser render PASS. Authenticated production feature E2E chưa claim vì không có test session credential trong turn này. Cross-tab lifecycle và ChatWidget overlap là residual.
- Agy execution lane tạo candidate đúng scope; Mika sửa test warning nhỏ sau reviewer note và verify lại focused/lint; không deploy/push.

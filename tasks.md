# KURABE — P103 executable plan / pending ledger

## Planning authority and entry gate

- `PLAN_REVISION=P103-r1`; source `ab4d6a798946ff26c565d17095685a1571688db9`; canonical `main` at `/home/pi5/projects/kurabe`.
- **P103M1T01, P103M1T02, P103M1T03, P103M2T01, P103M2T02 and P103M2T03 are completed; 9 P103 tasks remain pending.** This owner-requested WBS supersedes old “no open work” prose for P103 only. Historical IDs below remain historical, never redispatched.
- `.ai/MASTER_PLAN.md` sections 1–7 are mandatory context for each task. Rules A–D settle authorization, multi-team selection, workflow/status and snapshots; a worker must not pick a different policy.
- `Independent: yes` means no new P103 prerequisite; `no` means dependencies required. `Parallel-safe: yes` is a descriptive opportunity only after dependencies integrate and ownership/locks/live state allow it; it is not a scheduler bypass or runtime schema field.
- `READY_TO_EXECUTE=YES`: owner authorized bounded P103 execution; production write, deploy, credential change, and migration remain unauthorized unless separately approved.
- All tasks use isolated per-task WTs; Runner never edits `tasks.md`, `AGENTS.md`, `.ai/*`, `.state/*`, HANDOFF or protected owner changes. `owns` below includes matching tests, not control files. Existing source is context, not blanket write permission.
- No new board/daemon/control DB. Runner preference AGY then coder capability fallback; Mika verifies independently. CONTROLLED tasks need fresh selected reviewer/readonly AGY before canonical integration; P103M4T04 adds final cross-feature review, not a replacement for task gates.

## Common execution / acceptance contract (applies to every task)

1. Re-read assigned interface/files and integrated dependency SHAs; preserve BASE_SHA/diff/status. New path labels below mean **planned**, not currently existing tools. If an export/path/interface differs, stop and refresh contract, do not invent a replacement silently.
2. `EVIDENCE` = fresh absolute `/home/pi5/hermes-artifacts/kurabe-p103/<task-id>/<run-id>` created by approved execution; no credentials in logs/CLI/report. Runtime path/DB/ports are unique and loopback, no production fallback; one heavyweight verifier at once.
3. Test file `.test.ts` is discovered by existing `npm run test`. Suite modules export existing wrapper-compatible `run({rootDir,suite,options})`. Authenticated suite cases return `{name,status:'PASS',expected,actual}` after real assertion; `passed:true` only if all required cases pass. Failure/blocked exits nonzero with first useful failure.
4. New `tests/support/confirmation-runtime.mjs` exports `withConfirmationRuntime(options, callback)`, yielding local action/browser/SQL helpers and fixture aliases; manages exact runtime cleanup after evidence, never fixture responses. `--mode qualify --evidence <absolute>` qualifies harness only. Reuse `scripts/db-bootstrap.mjs`, existing app-auth bootstrap/browser helpers where compatible, no parallel harness framework.
5. Common **V0** gates on every changed candidate: `npm run test`; `npm run lint -- --no-cache`; `npm run typecheck`; `node scripts/scan-source-secrets.mjs`; `git diff --check`. Implementation tasks additionally run named suites below. Full root build + actual production Next browser required at P103M4T01 and final exact candidate; local unit-only cannot claim DB/auth/browser PASS.
6. Build must use isolated env: no copied source `.env.local` or inherited production client; `scripts/run-with-env.mjs` otherwise overrides shell env. Synthetic local password/session is generated by harness, real `loginAction` and `getCurrentUserAction` actor readback; no mocked auth/Next/RPC. Version/migration boundaries derive manifest/ledger, no date-skip heuristic.
7. Evidence on every task: source/base/candidate SHA, owned diff, exact commands/exit status, red-before/green-after where behavior changes, negative controls, sanitized JSON with enumerated case IDs, SQL before/after where touched, screenshot/DOM/console if UI; hash manifest, residual risk and review identity. No “PASS” inferred from number of cases or old P102 status.
8. Failure path: preserve first failure, classify setup vs product, two bounded evidence-driven repair attempts per mika-v3 then HALT. No task `DONE` until exact candidate canonically integrated; all checkboxes stay empty during planning. Dependency coupling changes require Mika plan update, not scope creep.
9. Local rollback: revert only owned unintegrated delta / restore disposable function capture or discard owned fixture. Never delete/rewind history or restore vulnerable production security behavior. Production rollback is matched-pair/containment with separate approval.

## Dependency map / parallel groups

`P103M1T01 → P103M1T02 → P103M1T03 → P103M2T01 → {P103M2T02,P103M2T03} → P103M3T01 → {P103M3T02,P103M3T03} → P103M4T01 → P103M4T02 → P103M4T03 → P103M4T04`.

Only parallel groups: **PG1 = M2T02 + M2T03**, **PG2 = M3T02 + M3T03**. Same RPC/`evaluation.ts`/`read.ts`/`use-db.ts` ownership is sequential through ancestors, never concurrent siblings. Isolated DB lock names below are per task; runtime resource cap still serializes heavy verification. Each integration must include all canonical ancestors.

### [x] [#P103M1T01] Qualify reusable disposable confirmation fixture

```yaml
task:
  id: P103M1T01
  tier: CONTROLLED
  depends: []
  owns: [tests/support/confirmation-runtime.mjs, tests/support/confirmation-fixtures.mjs, tests/confirmation-harness-contract.test.ts]
  locks: [P103M1T01_LOCAL_RUNTIME]
```
- **Independent:** yes. **Parallel-safe:** no (foundation).
- **Goal/current context:** reusable real stack for this bounded set, not re-audit. Reuse DB manifest/bootstrap, `tests/integration/app-auth-bootstrap.mjs`, `tests/browser/app-auth-harness.mjs` and preserved confirmation artifacts (read only, never import top-level old case runner side effects).
- **Interface:** common `withConfirmationRuntime(options, callback)` + qualify CLI above; fixture IDs represented by aliases, returned runtime helpers expose real login/action, SQL query and browser capture. API trace is forwarding/observability only, never response substitution.
- **Implementation steps:** (1) implement loopback/ownership/env/egress guard and manifest-ledger boot; (2) seed six users, A/B/C and V1/V2 config, create active/closed controls using Manager action; (3) verify all six sessions via actor readback; (4) package sanitized source/DB/runtime provenance and exact cleanup.
- **Acceptance criteria:** isolated Next/PostgREST/PG/auth really run; no production endpoints/env; baseline old failures kept distinct from harness qualification; no applied migration changes or disabled guards. H2 prerequisite workaround permitted only for recorded baseline and H4/H5 fixture before migration 002: temporary synthetic primary B for real assignment creation, finally restore A before measurement. After 002 it is forbidden; rerun H5 without workaround.
- **Mandatory tests / verification:** V0; `node tests/support/confirmation-runtime.mjs --mode qualify --evidence "$EVIDENCE/qualification.json"`; negative host/DB-name/missing-env/unknown ownership cleanup checks in unit harness contract. Qualify proves substrate, not H1–H7 fixed.
- **Evidence:** six actor-ID checks, bootstrap manifest and catalog/ACL digests, bound ports/container IDs, sanitized traffic, source SHA, cleanup readback; preserve first setup failure separately.
- **DB/security impact:** synthetic disposable writes only; credentials never emitted. No production bootstrap/client fallback.
- **Rollback/risk:** tear down exact owned local resources after preserving evidence; refuse ambiguous/shared targets. Missing Docker/Chrome/runtime capability => BLOCKED_CAPABILITY, not synthetic PASS.
- **Execution result:** PASS after fresh independent review; canonical task commit `4af6cd911b7c411b3a21243ad1dda49ea3d6ee38`; evidence `runtime-qualification-repair.json` and `candidate-verification-repair.json`; production writes `0`; disposable residue `0`.

### [x] [#P103M1T02] H4 server history authorization and target non-disclosure

```yaml
task:
  id: P103M1T02
  tier: CONTROLLED
  depends: [P103M1T01]
  owns: [src/lib/db/evaluation-history-admin.ts, 'src/app/history/[employeeId]/page.tsx', src/components/evaluation/EvaluationHistoryPage.tsx, src/data/workflow.ts, src/lib/db/teams-admin.ts, src/actions/read.ts, tests/evaluation-read-regression.test.mjs, tests/h4-history-authorization.test.ts, tests/integration/h4-history.mjs, tests/browser/h4-history.mjs]
  locks: [P103M1T02_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** no.
- **Goal/current context:** stop `getEvaluationHistoryAdmin` leaking target on empty/filtered history; trace `HistoryPage`, `canViewEvaluation`, `getLeaderTeamIds`. Preserve authorized historical snapshot reads.
- **Interface:** keep history result shape; denied => `{target:null,entries:[]}`, unauthorized/not-found same UI state. Current active Leader scope = active primary ∪ active appointed teams; other role checks preserved.
- **Implementation steps:** (1) add red HTML/RSC target-leak and permission truth table; (2) resolve server current scope once; (3) return target only for current target scope or at least one authorized closed-history entry; (4) prevent stale unfinished assignment outside scope granting access; (5) centralize route and `getEvaluationHistoryAction` policy; (6) enforce active primary + active appointed Leader scope; (7) render unavailable without target details; (8) positive-control historical evaluator, self/Manager/B.
- **Acceptance criteria:** A+B cannot see name/code/role/team of C with no/permitted-zero history; direct action and route consistent, errors fail closed; authorized history preserved, no global allow or client-only mask.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite h4-history --tier authenticated --evidence "$EVIDENCE/h4-history.json"`. Include missing target, empty/populated forbidden target, anonymous/individual/SubLeader, withdrawn unfinished assignment, old submitted evaluator and unrelated period denial.
- **Evidence:** baseline leak vs fixed redacted HTML/RSC/action payload, positive authorized history entries, denied payload field assertions, screenshots of generic unavailable and permitted B history.
- **DB/security impact:** no migration; scoped service-role read only in app, no new target enumeration.
- **Rollback/risk:** scoped local revert; historical over-denial prevented by positive snapshot cases. Do not deploy/revert to known leak as workaround.
- **Execution result:** PASS after fresh exact-SHA populated authenticated route/action qualification and independent `agy-readonly` review. Frozen candidate `7a7a759eef968ec7ec4204b8ae28e44155520815` integrated from canonical base `072d247413485c459f51d983105f95e393e76717` as `eddafaca9c197931499bd35edb7f1b5f28b84f50`; closure evidence `H4-final-closure.json`; production writes/migrations/deployment `0`; known H6 fixture interference remained unchanged and documented.

### [x] [#P103M1T03] H5 transactional current authorization after revoke

```yaml
task:
  id: P103M1T03
  tier: CONTROLLED
  depends: [P103M1T02]
  owns: [supabase/migrations/20260914000100_evaluation_current_authorization.sql, src/actions/evaluation.ts, src/data/workflow.ts, tests/h5-current-authorization.test.ts, tests/integration/h5-revoke.mjs]
  locks: [P103_EVALUATION_RPC_CONTRACT, P103M1T03_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** no.
- **Goal/current context:** stored assignee must not keep current B write rights after revoke. Exact actions `saveEvaluationRound`, `initializeEvaluationRoundDraft`, `returnEvaluationRound`; save/initialize call 17-arg `save_evaluation_round_transaction_active_only`, return calls `return_evaluation_round_transaction`.
- **Interface:** signatures/result shape unchanged; current active actor/role/team/relationship + stored assignment required. Saved evaluator/subject snapshots never rewritten to authorize current actor. Primary-A, revoked-B deny; active primary-team compatibility remains per master A2/A3.
- **Implementation steps:** (1) capture baseline failure via Manager revoke; (2) add server preflight where appropriate; (3) forward migration adds current authorization to save and return RPC, same existing personnel graph fence before period/config/evaluation locks; (4) keep legacy overload denial/ACL intact; (5) exercise initialize/return sibling paths so save cannot be bypassed; (6) races both orders with bounded timeouts.
- **Acceptance criteria:** after committed revoke old cookie draft/submit/initialize/return deny and entire DB before/after stays unchanged; current authorized B succeeds. Save-first may finish before revoke; revoke-first denies. No deadlock in controlled two-connection schedules; no widening Manager/SELF exceptions.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite h5-revoke --tier authenticated --evidence "$EVIDENCE/h5-revoke.json"`; fresh+upgrade migration 001 on disposable; wrong actor, inactive user/team, demotion, primary move, unrelated C, direct-RPC and legacy-overload denial. Positive Manager return/self return within existing rules.
- **Evidence:** current role/team/appointment before/after, action+RPC results, round/evaluation immutable-on-deny hashes, two-session lock sequence/wait timings, pre/post function definitions/signatures/ACL/search_path and only intended semantic changes.
- **DB/security impact:** migration 001 (NEW) replaces save/return function bodies, no data repair/schema expansion/GRANT broadening; source applied SQL remains byte-identical.
- **Rollback/risk:** existing personnel table-fence order reused; contention/period-close/config-writer race bounded and recorded. Disposable restore exact captured functions; production rollback may not resurrect revoked-authority bypass, use containment/forward correction with approval.
- **Execution result:** PASS after fresh exact-SHA authenticated qualification on candidate `6205ef475cd3a837697ff0d1fab932f914378482`; evidence `/home/pi5/hermes-artifacts/kurabe-p103/P103M1T03/H5-final-qualification.json` SHA-256 `b3bb127715118c204327a82cc4817083a4837795f216aaef99b2d414ca7ffe0b`; fresh independent review `PASS`; integrated canonical commit `2e2623af5dd01bef849f3ee713bea51e301b332f`; production writes/migrations `0`; known H6 response-contract interference unchanged and documented.

### [x] [#P103M2T01] H1/H2 workflow parity and multi-team SQL transition

```yaml
task:
  id: P103M2T01
  tier: CONTROLLED
  depends: [P103M1T03]
  owns: [supabase/migrations/20260914000200_evaluation_workflow_multiteam.sql, src/lib/evaluation-workflow.ts, src/lib/evaluator-resolver.ts, src/lib/team-validation.ts, tests/h1h2-workflow-contract.test.ts, tests/multi-team-leader-regression.test.ts, tests/integration/h1h2-workflow.mjs]
  locks: [P103_EVALUATION_RPC_CONTRACT, P103M2T01_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** no.
- **Goal/current context:** synchronize `getNextEvaluationStep`, `resolveEvaluatorFromDb`, `selectValidLeader` and save RPC; Employee/Worker SubLeader→Leader→Manager, valid appointed B independent of primary A.
- **Interface:** unchanged RPC signature, table C fixed for all five roles; master B deterministic next-Leader selection. No new workflow engine/generator/config API.
- **Implementation steps:** (1) write exact role/round table and real failure oracles; (2) migration 002 starts from function body after 001 and corrects next-role/eligibility without dropping H5; (3) validate appointed pointer first, unique primary fallback only when pointer absent, reject invalid/ambiguous candidates; (4) tests full valid flows and illegal transitions; (5) rerun H5 after 002.
- **Acceptance criteria:** all role flows reach correct final status/round; Employee/Worker cannot SELF R1; appointed primary-A Leader receives and saves B; unrelated C cannot be assigned. No fixture membership workaround on candidate. Actor/config/active-period/return guards survive.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite h1h2-workflow --tier authenticated --evidence "$EVIDENCE/h1h2-workflow.json"`; `node scripts/verify-release.mjs --suite h5-revoke --tier authenticated --evidence "$EVIDENCE/h5-after-002.json"`. Include no appointment, invalid/inactive pointer, multiple primary Leaders, skipped/backward/replayed/final-tampered submits, stale version and closed period.
- **Evidence:** ordered fresh/upgrade ledger 001→002, role/round expected-vs-actual tuples, RPC errors with rollback proof, cross-team allow/deny, save/return guard hashes, no legacy migration diffs.
- **DB/security impact:** migration 002 (NEW) replaces save body only; preserve 001 return guard unchanged; no historical score/evaluator updates.
- **Rollback/risk:** downgrade local 002 only to reviewed 001 snapshot for diagnosis; 001 remains authoritative security fence. New/old app/DB compatibility measured again at release preparation, no partial production rollout.
- **Execution result:** PASS on fresh exact-SHA authenticated runtime, candidate `a332e72737d055e38358f570363c13fe901e9e4b`; base `7a46233938e8a0636a388c9e0c8dda2b7373903a`; evidence `/home/pi5/hermes-artifacts/kurabe-p103/P103M2T01/H1H2-final-qualification-a332e727.json`, SHA-256 `5f6a7c51fef3f5abb94a3a3a0696a5c935e630b3d9fc340f598e86e93c9616af`; fresh read-only review `PASS`; canonical fast-forward integrated; production writes/migrations `0`; runtime residue `0`.

### [x] [#P103M2T02] H3 full/summary/single scope parity

```yaml
task:
  id: P103M2T02
  tier: CONTROLLED
  depends: [P103M2T01]
  owns: [src/lib/db/evaluations-admin.ts, src/lib/db/evaluations.ts, src/actions/read.ts, tests/h3-evaluation-scope.test.ts, tests/integration/h3-scope.mjs]
  locks: [P103M2T02_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** yes, only with P103M2T03 after P103M2T01 integrated.
- **Goal/current context:** `fetchEvaluationsForViewerAdmin` loses `leaderTeamIds` at `filterEvaluationsForViewer`; current evidence source-only, not runtime confirmation.
- **Interface:** existing read APIs unchanged; use explicit server-resolved context in full/summary/by-period/by-ID paths, master A/B and H4 historical policy preserved.
- **Implementation steps:** (1) fixture active secondary B evaluation with viewer not stored assignee, reproduce full-vs-summary red; (2) pass resolved scope through filter and sibling single/detail checks; (3) query errors fail closed; (4) assert independent expected ID sets under identical period/limit/window.
- **Acceptance criteria:** valid B IDs survive both SQL candidate query and post-filter; C absent; revoked B absent unless independently permitted historical read. No unfiltered fallback/N+1 or client-supplied scope authorization.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite h3-scope --tier authenticated --evidence "$EVIDENCE/h3-scope.json"`; rerun `h4-history` wrapper with evidence `$EVIDENCE/h4-after-h3.json`. Include own/Manager/SubLeader/A-only/A+B/C, zero assigned rounds, pagination and scope lookup failure.
- **Evidence:** explicit runtime status red baseline then green, sorted expected/actual IDs for each surface, query/error scope traces, negative C payload check. If not reproduced, preserve evidence and revisit minimal patch; do not fabricate confirmation.
- **DB/security impact:** none; service-role post-filter must remain mandatory.
- **Rollback/risk:** local owned diff revert; watch false parity caused by limit/projection differences. Never relax permission to make count equal.
- **Execution result:** PASS on fresh exact-SHA authenticated runtime, candidate `e91bfafba66960f74f3c98a12939f335344efc7c`; base `a72c0d0cee2ff196c5903fca9528d8d3aa1323ae`; evidence `/home/pi5/hermes-artifacts/kurabe-p103/P103M2T02-auth/H3-final-qualification.json` SHA-256 `0f6ce95a45124d41ec3dc8cffe6e2b4ae841e68e8b571166092cf14651b0bf92`; fresh independent `agy-readonly` review PASS at `/home/pi5/hermes-artifacts/kurabe-p103/P103M2T02/independent-review-e91bfaf-safe.log` SHA-256 `8993e901d75b257019951676720a2cb31ed2ce5e8275e9dab0f0ff9bb59fe9bc`; canonical fast-forward integrated at candidate SHA; production writes/migrations `0`; cleanup residue `0`.

### [x] [#P103M2T03] H6 draft transaction response contract

```yaml
task:
  id: P103M2T03
  tier: CONTROLLED
  depends: [P103M2T01]
  owns: [src/actions/evaluation.ts, tests/h6-draft-response.test.ts, tests/integration/h6-draft.mjs]
  locks: [P103M2T03_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** yes, only with P103M2T02.
- **Goal/current context:** `saveEvaluationRound` expectedStatus defaults Draft, conflating parent status. `isEvaluationTransactionResult` is local to **`src/actions/evaluation.ts`**, shared initialize result validation must be checked as well.
- **Interface:** preserve response shape and RPC tuple; successful draft returns success for correct aggregate R1 Draft / R2 Submitted / R3 Reviewed. ID/status structural validation retained; unknown outcome not automatically retried.
- **Implementation steps:** (1) failing real R2/R3 response+commit proof; (2) derive expected aggregate from operation/validated snapshot, repair save/initialize consumer parity only; (3) keep malformed/wrong ID/status rejects and fresh readback on uncertain outcome; (4) no submit audit/next transition for draft.
- **Acceptance criteria:** action response matches DB for all drafts/submits/final; repeated identical draft no extra round; initialization semantics not loosened; revoked/closed/stale writes fail atomically. Do not change parent status to Draft to match old validator.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite h6-draft --tier authenticated --evidence "$EVIDENCE/h6-draft.json"`; unit malformed/wrong-ID/impossible-status counterexamples; authentic initialize/draft/retry/final paths. No API mock substitutes for positive commit tests.
- **Evidence:** action response, before/after round score/comment/status/submitted_at and aggregate/next evaluator; malformed response units clearly labeled unit tier, no false authenticated evidence.
- **DB/security impact:** no migration; preserve H5 prechecks and strict server/RPC response guards.
- **Rollback/risk:** action-only local revert; never rollback committed draft data. Network after-commit failure remains outcome-unverified until readback, no duplicate submit retry.
- **Execution result:** PASS on fresh exact-SHA authenticated runtime, candidate `6514466e0e81f1ea33b5775ce315c35dd8fd467d`; base `a72c0d0cee2ff196c5903fca9528d8d3aa1323ae`; evidence `/home/pi5/hermes-artifacts/kurabe-p103/P103M2T03-auth/H6-final-qualification.json` SHA-256 `bd50bea4738b5fc8bfeb66fce5b6258d6953def0d52a650fef4cb37538aea748`; fresh independent `agy-readonly` review PASS at `/home/pi5/hermes-artifacts/kurabe-p103/P103M2T03/independent-review-6514466-package.log` SHA-256 `77af3dc2b9585b7ca3fe19ac2949ab2256c9a1f72cc2f1a288e1ce7bb33b0280`; exact candidate is canonical merge parent in integration commit `f60102fce8af98eff50564dff27023f3b6d16680`; production writes/migrations `0`; cleanup residue `0`.

### [ ] [#P103M3T01] H7 authorized per-round snapshot display DTO

```yaml
task:
  id: P103M3T01
  tier: CONTROLLED
  depends: [P103M2T02, P103M2T03]
  owns: [src/lib/db/evaluation-display-admin.ts, src/actions/read.ts, src/types/index.ts, src/hooks/use-db.ts, tests/h7-snapshot-display.test.ts, tests/integration/h7-display.mjs]
  locks: [P103M3T01_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** no (additive interface for both following tasks).
- **Goal/current context:** use existing immutable criteria tables / round version IDs and persisted grade, no reconstruction from current role/config. Read consumers must authorize evaluation before version lookup.
- **Interface (NEW):** `getEvaluationDisplayAdmin(evaluationId, viewer)` and server `getEvaluationDisplayAction(evaluationId)` plus `useEvaluationDisplay` hook. DTO exactly master D/H7: evaluation ID/subject role snapshot and per-round persisted grade/score/evaluator role/version IDs/snapshotState/criteriaGroups. Existing read payload remains compatible.
- **Implementation steps:** (1) add DTO and server loader using H3/H4 authorized evaluation; (2) batch unique version IDs and read immutable group/criterion/level/audience rows; (3) no arbitrary config-ID endpoint, grade uses persisted value; (4) null version=legacy_unknown, missing/error=unavailable, no live fallback; (5) hook wiring with existing requester+evaluation+period identity, final appointment fingerprint added by M3T03.
- **Acceptance criteria:** unauthorized C gets no snapshot DTO/metadata; config/role changes don't alter submitted DTO; per-round V1/V2 retained; missing version does not rewrite DB or invent labels; existing live criteria API untouched.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite h7-display --tier authenticated --evidence "$EVIDENCE/h7-display-server.json"`. Include different round versions, current-role change, null legacy refs, no-current-config, version-query failure and unauthorized ID.
- **Evidence:** DTO before/after Manager config/role changes, immutable submitted/closed SQL digests, unique-version query counts (batch, no per-criterion waterfall), denied response, legacy/error output.
- **DB/security impact:** no migration/new RPC; server-only version reads using existing permission path; no broad config dumps.
- **Rollback/risk:** additive DTO can revert locally without data rewrite. Broken required referenced version is surfaced; synthetic missing-row test must not disable FK guards (unit loader failure or valid legacy null fixture instead).

### [ ] [#P103M3T02] H7 detail/compare render historical snapshots

```yaml
task:
  id: P103M3T02
  tier: CONTROLLED
  depends: [P103M3T01]
  owns: ['src/app/evaluations/[id]/compare/ComparePageClient.tsx', 'src/app/evaluations/[id]/EvaluationPageClient.tsx', src/hooks/use-evaluation-page-state.ts, tests/h7-display-rendering.test.ts, tests/browser/h7-display.mjs]
  locks: [P103M3T02_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** yes, only with P103M3T03. Both consume fixed M3T01 interface; neither edits sibling-owned hook/types.
- **Goal/current context:** submitted compare/detail currently recompute with live bands/current user role and labels. Preserve existing composition/layout, change data binding and explicit missing/error states only.
- **Interface:** consume `useEvaluationDisplay` DTO for submitted rounds; draft remains current-rules workflow. Render different criteria version labels per round, don't let merged column overwrite historic labels.
- **Implementation steps:** (1) red browser settled screenshot/DOM for 170/S→D baseline; (2) submitted score/grade/criteria use DTO, subject snapshot role not current profile; (3) loading/error/legacy states without live-config flicker; (4) preserve compare active-period gate/history route boundary, draft edit controls; (5) mobile/desktop and keyboard smoke on affected views.
- **Acceptance criteria:** Manager grade/criteria V2 and role changes cannot change submitted values; different per-round labels retained; draft displays current values + stale guard. No DB mutation, no opening closed compare as shortcut; delayed/error snapshot never shows wrong latest grade.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite h7-display --tier authenticated --evidence "$EVIDENCE/h7-display-browser.json"` (now server+browser modules); settled DOM+screenshot at desktop/mobile, loading/error/empty/legacy, draft save control test.
- **Evidence:** before/after grade/criteria/role screenshots with stable-ready conditions, exact employee route ID vs evaluation action ID, browser console, same DB snapshots, inaccessible C UI.
- **DB/security impact:** none; UI never becomes authorization authority, historical display not editable.
- **Rollback/risk:** local component binding revert only; no current-config fallback for missing snapshot; no global scoring helper rewrite or design refactor.

### [ ] [#P103M3T03] Scope-aware cache and resident revoke freshness

```yaml
task:
  id: P103M3T03
  tier: CONTROLLED
  depends: [P103M3T01]
  owns: [src/actions/read.ts, src/types/index.ts, src/hooks/use-db.ts, src/contexts/AuthContext.tsx, src/providers/query-provider.tsx, tests/p103-requester-cache.test.ts, tests/browser/cache-confirmation.mjs]
  locks: [P103M3T03_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** yes, only with P103M3T02.
- **Goal/current context:** `requesterScope` only id/role/primary; grant/revoke B with primary A unchanged must affect data/cache. Preserve M3T01 snapshot hook interface for concurrent UI task.
- **Interface (NEW):** `getViewerScopeAction()` returns `{userId,role,primaryTeamId,leaderTeamIds,scopeKey}` from session/server helper, sorted stable scope key. Add context fields without changing old consumers; server never trusts client key for ACL.
- **Implementation steps:** (1) server scope payload + auth context reconciliation; (2) extend all related requester keys/invalidation with scope and period/evaluation/version identity; (3) cancel/remove old keys on change/logout/error, epoch fence old in-flight completion; (4) focus/navigation/auth + visible-tab max 30s scope refresh, hidden tab reconcile before render; (5) targeted mutation invalidation for H6 drafts and live config, immutable H7 versions retained.
- **Acceptance criteria:** resident revoke/grant B reflected without changing primary or relogin; background response cannot restore old B data; login/user/period switch cannot reuse previous scope; query error removes sensitive last-good view. Remote revoke write deny immediate in DB; UI bound <=30s, not instant push.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite cache-confirmation --tier authenticated --evidence "$EVIDENCE/cache-confirmation.json"`. Use two real sessions (Manager mutation, resident Leader), controlled delayed response, focus/background/error/logout/current-user/period changes; verify H6 fresh draft read and H7 snapshot stability.
- **Evidence:** sorted key before/after with IDs redacted to aliases, timings bound, delayed-response timeline, no stale DOM screenshots, request counts proving no hidden-tab polling/broad fetch loop.
- **DB/security impact:** none; no new table/event bus; server/RPC auth remains authoritative on every read/write.
- **Rollback/risk:** async cancellation and network errors; fail-closed UI rather than reusing revoked cache. Do not invalidate every user globally or add polling infrastructure.

### [ ] [#P103M4T01] Full integration and actual production-Next E2E matrix

```yaml
task:
  id: P103M4T01
  tier: CONTROLLED
  depends: [P103M3T02, P103M3T03]
  owns: [tests/integration/confirmation-matrix.mjs, tests/browser/confirmation-matrix.mjs, tests/operations/p103-required-cases.mjs]
  locks: [P103M4T01_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** no (joins UI/cache).
- **Goal/current context:** combined real run of H1–H7, not seven isolated green labels. Reuse already integrated case modules and T01 fixture; don't edit them from outside owns.
- **Interface (NEW):** wrapper suite `confirmation-matrix`; `p103-required-cases.mjs` exports frozen required case IDs consumed by matrix and later CI. Separate DB/browser IDs, no duplicated results masquerading as extra cases.
- **Implementation steps:** (1) enumerate required IDs from matrix below; (2) execute every case through real build/start/login/action/RPC/browser and capture exact artifact digests; (3) real lifecycle active→submitted→closed controls and grant/revoke/return/draft/config/role; (4) run new+existing relevant suites/root gates on exact candidate; (5) preserve fixture until matrix finished, cleanup only owned runtime after evidence.
- **Acceptance criteria:** no case skipped/blocked/mock; no membership workaround after H2; all allow/deny/atomicity/snapshot/cache assertions pass. DB rejects leave rows identical; recorded credentials/auth never enter report. Zero unexplained browser/Next errors, no out-of-scope target data.
- **Mandatory tests / verification:** V0 + `npm run build`; `node scripts/verify-release.mjs --suite confirmation-matrix --tier authenticated --evidence "$EVIDENCE/confirmation-matrix.json"`; `node scripts/verify-release.mjs --suite evaluation-transition-guard --evidence "$EVIDENCE/existing-transition.json"`; `KURABE_RELEASE_DB_ENABLED=1 node scripts/verify-release.mjs --suite release-matrix --evidence "$EVIDENCE/existing-release.json"`. Existing release modules have different native tiers (integration real-DB, browser separate); do not force all to authenticated. Old source/bootstrap guards are supplemental, not proof 001/002 ran; new matrix validates candidate bodies directly. No unconfigured `personnel-actor-guard-real` call to default port 5432; new h5-revoke covers the actual protected path on owned local DB.
- **Evidence:** exact built source/candidate SHA, migration hashes/ledger/ACL, required case ID set, action+DB/browser linkage, closed snapshot digests, old+new suite outputs, full console/readiness and teardown proof.
- **DB/security impact:** disposable changes only, current auth and historical immutability cross-feature checks.
- **Rollback/risk:** no production writes; first failure retained even if targeted suites green. Combined failure returns to owning implementation task through Mika, not stealth patch in verifier task.

### [ ] [#P103M4T02] CI fail-closed enforcement for confirmation evidence

```yaml
task:
  id: P103M4T02
  tier: CONTROLLED
  depends: [P103M4T01]
  owns: [scripts/verify-release.mjs, tests/verify-release-confirmation.test.ts, tests/operations/ci-suite-manifest.mjs, .github/workflows/ci.yml]
  locks: [P103M4T02_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** no (consumes actual M4T01 schema/IDs).
- **Goal/current context:** `validateSuiteResult` currently ignores contradictory case/top-level status; prevent false-green safety fixes without redesigning CI.
- **Interface:** new matrix must match required case IDs and authenticated runtime provenance; legacy suites keep documented shape unless contradicting explicit FAIL. New matrix evidence has exact source/candidate SHA and artifact hashes verified from files, not merely trusted metadata.
- **Implementation steps:** (1) negative tests top passed=true but status/case FAIL; (2) reject zero/missing/duplicate/skipped required cases, insufficient tier, wrong SHA/hash or absent evidence; (3) CI provisions only disposable stack and invokes real `confirmation-matrix`; (4) keep source/mock suites classified, not substitutes; (5) retain sanitized artifacts on failure and nonzero exit through job/wrapper.
- **Acceptance criteria:** poisoned evidence always rejected, genuine matrix accepted, old unrelated valid suite formats not silently discarded; CI has no production credentials, no continue-on-error/allow-failure for mandatory cases.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite ci-suite-manifest --evidence "$EVIDENCE/ci-manifest.json"`; `node scripts/verify-release.mjs --suite confirmation-matrix --tier authenticated --evidence "$EVIDENCE/ci-local-matrix.json"`; negative evidence tests via `npm run test`. Remote CI only if publication separately approved; local CI-equivalent run is not claimed remote green.
- **Evidence:** each poisoned payload + rejection, valid matrix acceptance, exact workflow commands/diff, unchanged classification of existing suites, real local CI-equivalent log and remote URL/SHA only if actually run.
- **DB/security impact:** no application/migration changes; isolated CI fixtures only, no secret artifact uploads.
- **Rollback/risk:** preserve evidence enforcement; avoid broad evidence-schema rewrite breaking unrelated suites. If remote CI not available, mark remote gate UNKNOWN/pending, no fake PASS.

### [ ] [#P103M4T03] Matched release package and disposable rollback rehearsal

```yaml
task:
  id: P103M4T03
  tier: CONTROLLED
  depends: [P103M4T02]
  owns: [docs/P103_RELEASE_CHECKLIST.md, tests/operations/p103-release-preflight.mjs, tests/integration/release-matrix.mjs]
  locks: [P103M4T03_LOCAL_RUNTIME]
```
- **Independent:** no. **Parallel-safe:** no.
- **Goal/current context:** package verified app+001+002 as inseparable release set, preserve existing manifest/rollback machinery. No edit owner-modified `docs/PRODUCTION_RUNBOOK.md`.
- **Interface:** `buildManifest/buildRollbackManifest` existing consumers remain compatible; include 001/002 exact hashes/overload/ACL/provenance. New local `p103-release-preflight` suite never applies to external target. New release checklist is application docs, not `.ai` control authority.
- **Implementation steps:** (1) refresh final catalog and manifest from disposable candidate, detect historical applied migration edits; (2) old/new app × baseline/001/002 compatibility rehearsal, record vulnerable combinations as not release-eligible; (3) simulate interrupted apply/build and exact function capture/restore on disposable, ensure rollback doesn't pretend to preserve new protections; (4) package safe forward-fix/write-pause fallback and production no-write/read-only-smoke envelope; (5) rerun existing manifest/preflight gates against updated manifest.
- **Acceptance criteria:** compatibility table factual, rollback/containment usable, snapshot/data untouched, security downgrade not designated safe rollback; app/migrations/evidence bound to exact candidate, no deploy performed. If current production catalog unavailable, preparation can finish but live drift gate stays UNKNOWN and blocks release, not silently default to old confirmation.
- **Mandatory tests / verification:** V0; `node scripts/verify-release.mjs --suite p103-release-preflight --evidence "$EVIDENCE/p103-release-preflight.json"`; `node scripts/verify-release.mjs --suite release-manifest-integrity --evidence "$EVIDENCE/release-manifest-integrity.json"`; `KURABE_RELEASE_DB_ENABLED=1 node scripts/verify-release.mjs --suite release-preflight --evidence "$EVIDENCE/existing-preflight.json"`; rerun `confirmation-matrix` after final manifest/test changes. T01 harness provisions isolated env/capabilities required by existing preflight; no production connection.
- **Evidence:** exact source/tree/migration/ACL/function/evidence hashes, compatibility and rehearsal outputs, rollback limitations, no-write production query template, pending approvals and remote CI status. No private data dump in package.
- **DB/security impact:** fixture migration/rollback only; production apply/backup/deploy/write-pause require new explicit approval.
- **Rollback/risk:** new DB + old app can reintroduce H4 client/server read leak even if H5 SQL stays; don't treat old app as safe universal fallback. After real user writes, no wholesale DB restore or snapshot rewrite.

### [ ] [#P103M4T04] Fresh independent cross-feature review and release gate

```yaml
task:
  id: P103M4T04
  tier: CONTROLLED
  depends: [P103M4T03]
  owns: []
  locks: []
```
- **Independent:** no. **Parallel-safe:** no (final exact candidate gate).
- **Goal/current context:** fresh read-only reviewer independent of implementer reviews exact candidate/evidence, not Runner transcript or historical P102 PASS. This is a review deliverable, no application write scope.
- **Interface:** verdict `PASS|REJECT`, exact candidate/tree + migration hashes + evidence digests, findings with file/function/repro/risk, required changes, unresolved release conditions. Mika owns integration/control updates separately under approved protocol.
- **Implementation steps:** (1) verify prerequisites/evidence integrity and no dirty/foreign paths; (2) re-evaluate H4/H5 current auth vs history, full TS/SQL/team/cache semantic, H6 response, H7 snapshot and race/rollback coverage; (3) risk-relevant independent checks, reject unexplained failures; (4) adjudicate review findings via owning task; (5) publish release decision with separate approval envelope, never deploy in review.
- **Acceptance criteria:** no unresolved blocking HIGH/security/data finding; all required matrix IDs executed, no stale SHA verdict; review PASS only about verified candidate. Production gate stays closed until explicit owner approval, fresh live catalog/env readback and required remote CI (if release route requires it).
- **Mandatory tests / verification:** Mika V0 + `npm run build` and `node scripts/verify-release.mjs --suite confirmation-matrix --tier authenticated --evidence "$EVIDENCE/final-exact-candidate.json"` before review; reviewer independently verifies risk-selected raw evidence, checksums and boundaries without tracked edits. Changed candidate/base/evidence => fresh gates/review, not reuse PASS.
- **Evidence:** fresh session/process identity, exact reviewed SHA/fingerprint, verdict/findings, verified manifest/required case set, explicit `IMPLEMENTATION_READY` vs `PRODUCTION_APPROVED` vs `REMOTE_CI` statuses.
- **DB/security impact:** none; read-only review, no production mutation. No debug fixture exception becomes production capability.
- **Rollback/risk:** REJECT routes bounded rework to owning task; no self-approval or review on a moving candidate. This task remains pending at planning like every other task.

## Required test matrix / acceptance coverage

| Coverage | Required behavior / negative control | Owning task / suite |
|---|---|---|
| Harness | Six real password/session identities; loopback guard; no env pollution; fresh+upgrade DB | M1T01 / qualify |
| H4 | Empty/populated forbidden target non-disclosure in HTML/RSC/action; self/Manager/B/historical allowed | M1T02 / h4-history |
| H5 | Revoke old cookie draft/submit/initialize/return deny; active positive; races both orders; atomic rollback | M1T03 / h5-revoke |
| H1 | Employee/Worker three rounds; all five role flows; role/next/final parity; stale/closed/skip deny | M2T01 / h1h2-workflow |
| H2 | Primary A appointed B select/submit/write; C deny; invalid/null/multiple candidate cases | M2T01 / h1h2-workflow |
| H3 | Baseline runtime red; full/summary/by-period/by-ID expected-ID parity; scope lookup fail closed | M2T02 / h3-scope |
| H6 | R1/2/3 draft response↔DB parity, no extra round; malformed/wrong tuple deny; initialize/submit unaffected | M2T03 / h6-draft |
| H7 server | Snapshot DTO before/after config+role, per-round versions, legacy/error/unauthorized cases | M3T01 / h7-display |
| H7 browser | Submitted grade/label stable; draft latest; loading/error/legacy; desktop/mobile; closed history unchanged | M3T02 / h7-display |
| Cache | Resident grant/revoke; <=30s visible bound; focus/hidden/error/logout/user/period; delayed old response | M3T03 / cache-confirmation |
| Integrated | Real Next production build+auth+RPC, return/draft/lifecycle/config, no workaround/mock | M4T01 / confirmation-matrix |
| CI | Contradictory FAIL/missing/duplicate/skip/wrong-tier/SHA/hash rejected; real matrix required | M4T02 / ci-suite-manifest + unit |
| Release | Baseline→001→002, old/new app pairs, captured rollback/forward-containment, no historical writes | M4T03 / p103-release-preflight + existing gates |
| Review | Fresh exact candidate, no blocking findings, owner/production/remote-CI gates explicit | M4T04 / independent review |

All short task IDs in this table carry `P103` prefix. Required individual case IDs are implemented/enumerated at M4T01; numeric case totals must derive from those IDs, not this category count.

---

<details>
<summary>Historical P102 control ledger — retained IDs, not P103 execution state</summary>

# KURABE — historical control ledger

## Authority / final status

- `PROJECT_STATE=COMPLETE`; this file is the canonical status ledger after the final production release and control/documentation sweep.
- Production/source SHA: `76221ca0a3e2813a2743a0ad74d3be7fb57306d4`; current repository `HEAD` and `origin/main` are the documentation closure commit.
- Production is LIVE and the release is COMPLETE. Production closure evidence: `/home/pi5/hermes-artifacts/kurabe-execution/production-release-closure-76221ca.log`.
- `.ai/MASTER_PLAN.md` owns current outcomes/invariants; this file owns the compact classification of every task retained by the former active WBS. Detailed historical task contracts remain recoverable in Git, `.state/agent-state.json`, and timestamped evidence.
- No active Runner, candidate, reservation, task/integration worktree, quarantine, or actionable `RUNNING`/`READY`/`BLOCKED`/`UNKNOWN` task remains.
- `MANDATORY_OPEN_WORK=NONE`.
- Protected owner changes remain untouched: `AGENTS.md` and `docs/PRODUCTION_RUNBOOK.md`.

## Completed P102M3 and release milestones

| Task / milestone | Classification | Canonical anchor or evidence |
|---|---|---|
| P102M3T01 — fail-closed verification/evidence contracts | DONE | Recorded PASS in `.state/agent-state.json`; canonical history/evidence under `/home/pi5/hermes-artifacts/kurabe-execution/` |
| P102M3T02 — credential/session lifecycle | DONE | Recorded PASS; production session/credential behavior and cleanup also PASS |
| P102M3T03 — login admission/proxy trust | DONE | Recorded PASS; production migration/readback included in release closure |
| P102M3T04 — evaluation transition/config versioning | DONE | Recorded PASS; production schema compatibility PASS |
| P102M3T05 — personnel actor scope/deletion graph | DONE | Recorded PASS; production personnel transaction repair/readback PASS |
| P102M3T06 — persistent AI summary coverage | DONE | Recorded PASS; release package and paired rollback evidence retained |
| P102M3T07 — AI egress/response boundary | DONE | Recorded PASS in completion history |
| P102M3T08 — viewer cache, mutation errors, resident freshness | DONE | Canonical commit `680b0efcd42ad581a6d03158fcd996b608386715`; browser/source-contract evidence retained |
| P102M3T09 — authenticated release matrix/CI enforcement | DONE | Canonical commit `8c76269834d6c13fdda2dfd1f0a3fc35e00f01a8`; release matrix PASS |
| P102M3T10 — measured performance baseline/optimization | DONE | Canonical commit `4efe42d979e231299663c669d0573199a58b0351`; performance evidence retained |
| P102M3T11 — final release/rollback package | DONE | Release package review PASS; later production release closure supersedes its pre-release source anchor |
| P102M3T12 — stale runtime/control/workspace reconciliation | DONE | Worktree cleanup PASS; only canonical repository remains |
| P102M3T13 — reusable authenticated local fixture | DONE | Fixture lane PASS and fully cleaned |
| Production migrations/schema compatibility | DONE | Production migration ledger/readback PASS; no production rollback required |
| Production deployment and alias cutover | DONE | Source `76221ca0a3e2813a2743a0ad74d3be7fb57306d4`; deployment READY; alias `https://lykiv.vercel.app` |
| Authenticated production smoke | DONE | Login/dashboard/read/deny matrix PASS; `HTTP_5XX=0`, `PAGE_ERRORS=0`, `VISIBLE_APP_ERRORS=0` |
| Multi-team Leader contract | DONE | `users.team_id` is primary membership; `teams.leader_id` is leadership assignment; production QI-KIV2 read PASS |
| Temporary credential cleanup | DONE | Reset RPC PASS; `SESSIONS=0`, `ACTIVE_SETUP_TOKENS=0`, no secret file retained |
| Kurabe worktree cleanup | DONE | 30 registered noncanonical worktrees + 11 stale sibling paths removed; only canonical repository remains |

## Retained IDs: future, owner-gated, or superseded

These are not current execution tasks. They remain only for traceability and must not be auto-dispatched.

| Task | Classification | Boundary / reason |
|---|---|---|
| P98M2T05 — old auth/framework rollout lane | OBSOLETE/SUPERSEDED | Replaced by the actual optional-password production release path. Strict password go-live remains separately deferred; no global forced setup is implied. |
| P98M3T02 — old evaluator NULL-safety rollout lane | OBSOLETE/SUPERSEDED | Superseded by the integrated evaluation guards, migration package, production schema/readback, and final release path. |
| P102M2T01 — branch-protection/settings lane | OWNER-GATED | Optional repository-settings governance is not required to keep the released runtime live; execute only after explicit owner approval and a fresh settings readback. |
| P102M2T02 — final release evidence/durable closure | DONE | Closed by production release evidence, worktree cleanup, and this final control/documentation sweep. |
| P96T11 — controlled lifecycle transition | DEFERRED / OWNER-GATED | No lifecycle mutation was required for this release; do not create or close production periods without a separate approved envelope. |
| P96T12 — stale/Closed behavior on run-created data | DEFERRED / OWNER-GATED | Depends on the deferred lifecycle envelope and exact approved fixture IDs. |
| P96T13 — exact-ID lifecycle rollback | DEFERRED / OWNER-GATED | Recovery companion for the deferred lifecycle envelope; not evidence of a current failure. |

## Final operating rule

Do not reopen completed IDs or create a new phase from this ledger. Optional improvements, strict-password rollout, repository settings, and lifecycle testing require a new explicit owner request with a fresh scope/approval boundary. Until then, no automatic task dispatch is authorized.

</details>

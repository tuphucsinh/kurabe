# Decisions Log

| # | Ngày | Quyết định | Lý do |
|---|---|---|---|
| 1 | 2026-04-27 | Chọn **TailwindCSS** thay Vanilla CSS | Tối ưu tốc độ vibe-code với AI, utility-first giảm context switching |
| 2 | 2026-04-27 | Chọn **Supabase** cho Backend (phase sau) | User chốt; giao diện local mock data trước |
| 3 | 2026-04-27 | Chọn **Next.js 15 App Router** | SSR/SSG sẵn, file-based routing, React Server Components |
| 4 | 2026-04-27 | Design System lấy từ **Stitch Project 1003102391417666898** | Đảm bảo UI khớp 100% bản mẫu đã thiết kế |
| 5 | 2026-04-28 | **Migrate criteria vào Supabase DB** | CRUD UI (Phase 22-23) cần DB thực tế; 34 tiêu chí + levels vào bảng `criteria`, `criterion_levels` |
| 6 | 2026-04-28 | **Giữ fake login** (chọn user từ list), query từ Supabase | Chưa cần Supabase Auth; chuyển sang phase riêng khi cần |
| 7 | 2026-04-28 | **Chưa enable RLS** — auth ở application level | RLS sẽ thêm sau khi có Supabase Auth thực sự |
| 8 | 2026-08-10 | **Tách Toast ra khỏi Data Layer** | Tránh gây lỗi `set-state-in-effect` và loop khi show notification từ hook/actions. Các components/actions return lỗi hoặc dùng context. |
| 9 | 2026-08-10 | **Bỏ qua Supabase Auth (Tạm hoãn Phase 44)** | Đang trong giai đoạn testing, cần user đăng nhập không password để dễ dàng sửa và thử nghiệm. |
| 10 | 2026-08-12 | **Tiếp quản dự án vào workflow chuẩn Mika** — sync AGENTS.md template chuẩn + xóa CLAUDE.md/GEMINI.md (Antigravity cũ). Baseline: `cef098f` | Đưa dự án vào quy trình Mika→Runner→Reviewer thống nhất; verify build PASS + lint PASS (1 warning). Phase 44 (Security) vẫn DEFERRED theo yêu cầu UAT. |
| 11 | 2026-08-14 | **Trả lại đánh giá (Phase 64)**: reviewer ở lượt chưa submit → trả về round-1 (unlock + xóa round hiện tại); Manager Approved tự trả về Draft; lưu `return_note` (bắt buộc nhập lý do); round hiện tại bị xóa khi trả lại | Cấp trên cần yêu cầu vòng trước chỉnh sửa; manager cần sửa báo cáo sau khi nộp; lý do bắt buộc để vòng trước biết cần sửa gì |
| 12 | 2026-08-14 | **Bật đăng nhập mật khẩu thật (Phase 69)**: login qua server action (bcrypt compare, cookie `auth_session` httpOnly — giữ tên cookie); rule NULL = login mã NV thuần (dự phòng); KHÔNG đặt pass sẵn cho account nào — NV tự đặt sau; `REVOKE SELECT (password_hash) FROM anon` để hash không lộ qua API | Hoàn tất P44-C1: thay mock login bằng password thật an toàn tối thiểu, giữ lối vào dự phòng không bao giờ khóa account |
| 13 | 2026-08-14 | **C3 — Server actions là lớp ghi DUY NHẤT (Phase 70)**: siết 8 bảng data chính từ "Enable all access for anon" → SELECT-only; mọi write qua server actions (requireAuth/requireManager + supabaseAdmin + logAudit + revalidate); migration theo NHÓM bảng (users/teams → criteria → evaluations) kèm test anon-blocked ngay sau mỗi nhóm chống regression im lặng; giữ nguyên mức quyền nghiệp vụ hiện tại (chỉ chuyển lớp) | Anon key công khai trong bundle → ai cũng ghi/xóa data; đóng lỗ hổng trước khi mở production; bài học P65T06 (anon DELETE fail im lặng) → migration từng nhóm + verify ngay |
| 14 | 2026-08-16 | **Team-detail redesign CHỈ mobile** (Phase 79 T2): desktop (md+) giữ NGUYÊN bản cũ (status badges + icon đánh giá + previousRounds + grid 4 cột); mobile gọn (bỏ icon/badge, chỉ điểm cuối). Dùng `max-md:hidden`/`md:hidden` (KHÔNG `hidden md:flex` — xung đột display class Tailwind) | Anh yêu cầu sau khi xem bản mobile: PC quen dùng bản cũ, mobile cần gọn |
| 15 | 2026-08-16 | **Mobile hiển thị text thuần, không badge/box/pill** (Phase 79 T2FIX2): giá trị role/grade/điểm là text có nhãn ("Mã:", "Vai trò:", "Xếp loại:", "Vòng Lx:", "điểm") — bỏ mọi box màu/viền trên mobile | Anh chốt: badge/label/box trên mobile khó nhìn; desktop giữ nguyên |
| 16 | 2026-08-16 | **Reviewer review diff UI lớn: KHÔNG mở browser** — dùng git diff + evidence screenshots/metrics có sẵn, trả lời <300 từ | 2 vòng reviewer timeout 600s vì tự mở browser-playwright nhiều lần; đổi sang evidence-based review chạy 60-240s |
| 17 | 2026-08-16 | **Duyệt plan refactor ZCode (REFACTOR_PLAN.md) — theo khuyến nghị Mika + agy Opus 4.6**: Q1 GIỮ anon-read (nội bộ) + vá B1/B2/B4; Q2 RLS đã đóng (13/13 bảng relrowsecurity=true + 0 policy write anon — verified DB thật) → B3 chỉ dọn REVOKE write anon; Q3 GIỮ luật cũ NULL=login mã NV (không ép pass) + B1 session thật; Q4 nâng xlsx 0.20.x CDN (CVE); Q5 gộp thông báo login; Q6 D3+D6 làm luôn đợt này. A1: grade_bands DB == hardcode (verified) → KHÔNG cần fix data cũ, chỉ fix code chống bom hẹn giờ. Thứ tự: A → B1/B4/B3-dọn → C → D | Plan ZCode 36/38 mục đúng (agy verified), mọi mục CAO Mika verify khớp code thật; anon-read là thiết kế nội bộ chấp nhận được, ưu tiên vá session thật trước |
| 18 | 2026-08-19 | **PIVOT — KHÔNG dùng PPR/cacheComponents cho Kurabe (Phase 87)**: Next 16 đã REMOVE `experimental_ppr` segment + deprecate `experimental.ppr`, gộp vào `cacheComponents` — flag TOÀN APP, ép mọi data access phải nằm trong Suspense/khai báo cache; build FAIL ngay (`/evaluations/[id]/compare`: "Uncached data was accessed outside of Suspense" tại AppLayout useAuth/providers ngoài Suspense) → đòi restructure layout/providers toàn app, rủi ro regression cao, không phải pilot 1 trang. Static shell đã được cache qua JS bundle immutable (_next/static) + route config đã static (criteria/login/support); unstable_cache từng FAIL (Fix A 16-08). Hướng còn lại có giá trị: giảm roundtrip server actions (gom batch query) — để sau khi anh duyệt | Pilot evidence-first: build fail là bằng chứng quyết định; không đẩy PPR, rollback sạch (revert 1 commit) |
| 19 | 2026-08-19 | **Gom server actions giảm roundtrip (Phase 88)**: 5 trang gom nhiều action riêng → 1 aggregate action per page (/employees 3→1, /teams 3→1, /teams/[id] 3→1, /evaluations/[id] 4→1, /compare 4→1) — 1 requireAuth + Promise.allSettled + per-part `*Error` field (không silent-empty), fail-closed giữ (eval null khi không quyền), giữ generationRef/nơi cần, invalidate đúng key. Kết quả: 13→5 request mount (tiết kiệm 2-3 chặn VN→SG→HK mỗi load). KHÔNG gom trang static nhẹ (criteria/settings/support: static prerender, ≤1 request). Test suite 10/10 PASS (fix 2 test cũ). Timing thật production UNKNOWN (cần DevTools) | Anh yêu cầu giảm request sau khi đo chậm Vercel; pattern giữ fail-closed + per-part error phòng lỗi im lặng |
| 24 | 2026-08-24 | **Chọn transactional RPC làm write path chính cho evaluation sau production canary PASS**: giữ feature flag ON; RPC transaction xử lý atomic round/evaluation transition, alias-qualified repair đã sửa lỗi `42702`; failure-path rollback và full 3-round UI canary PASS. Retention/purge/cron vẫn tách riêng, chưa chạy | RPC tăng integrity/concurrency safety; chỉ bật sau provenance, reviewer, local gates, authenticated canary và exact restore PASS |
| 25 | 2026-08-26 | **Evaluation draft hydrate phải canonical trước mọi write**: first-open chỉ init round editable `NotStarted`/pristine bằng conditional server action; hydrate carry-forward phải bổ sung `selectedLevelIndexes` bằng match score với criterion level; AI nhận xét đọc live current round qua `buildResultPrompt` | Ngăn autosave/lưu nháp bị validator từ chối hoặc overwrite race; giữ validator fail-closed và tránh dùng `lastRound` làm dữ liệu hiện tại |
| 26 | 2026-08-26 | **Giữ hai tín hiệu lịch sử trên evaluation card**: viền ngoài phản ánh thay đổi so với vòng gần nhất; badge mã tiêu chí phản ánh việc từng khác bất kỳ vòng trước nào | Anh chốt giữ nguyên để phân biệt trạng thái thay đổi hiện tại với việc tiêu chí từng có biến động trong lịch sử; không sửa CSS/logic |
| 27 | 2026-08-26 | **Staged loading evaluation detail theo dependency order**: giữ aggregate auth/evaluation boundary nhưng loại full users list khỏi critical path; render light employee/evaluation context trước criteria editor; mỗi layer có loading/error/retry/stale guard riêng | Full users query và criteria fetch đang chặn first usable view; đây là thay đổi nhỏ hơn split nhiều server actions, không đụng scoring/save/submit/RBAC/DB schema |
| 28 | 2026-08-26 | **Phase 95 dùng shared stateless `EvaluationStaticFrame` để đạt static-first tối thiểu**: route fallback và client loading dùng cùng frame có labels/structure thật; không có form/button/handler/state; periods chỉ defer sau authenticated waterfall chứng minh cần thiết | Skeleton-only chưa đủ; frame thật phải render trước `pageData` nhưng không được tạo duplicate stateful DOM hoặc lộ action theo quyền trước accessState |
| 29 | 2026-08-26 | **Compare page phải có performance workstream riêng, không chỉ UI compact**: đo cold/warm waterfall theo `shell → light → primary → secondary → full`; chỉ split query/cache/prefetch khi baseline chứng minh lợi ích ròng; ưu tiên static frame + staged primary rendering + unchanged collapse | Trang compare hiện global loading gate và aggregate action `employee/evaluation/users/groups`; chỉ sửa card/CSS chưa chứng minh first-useful hoặc full-load cải thiện, còn tách query sớm có duplicate/race/stale/RBAC risk |
| 30 | 2026-08-26 | **SUPERSEDED bởi #31** — bản nháp trước đây đề xuất `explicit period → Active → latest fallback → none` | Sau Sequential Thinking, latest fallback bị loại khỏi default detail/compare vì có thể mở kỳ đóng và gây nhầm/sửa sai; xem policy Active-only tại #31 |
| 31 | 2026-08-26 | **Điều chỉnh contract detail/compare thành Active-only mặc định**: không có Active → `NO_ACTIVE_PERIOD` rõ ràng; nhiều Active → fail-closed anomaly; kỳ đóng không tự fallback và chỉ dành cho future `Lịch sử đánh giá` read-only | Anh ưu tiên đơn giản, dễ hiểu, tránh người dùng mở nhầm kỳ đóng; fallback kỳ mới nhất có nguy cơ stale và detail write path hiện chưa kiểm tra period status |
| 32 | 2026-08-26 | **Active scope phải được resolve server-side trước detail/compare query và chứa ID thật trong React Query key**: resolver đọc tối đa 2 Active để phân biệt `NO_ACTIVE_PERIOD`/`MULTIPLE_ACTIVE_PERIODS`; aggregate re-validate `period_id` | `getActivePeriod().limit(1).single()` hiện âm thầm lấy một row nếu nhiều Active vì limit chạy trước single; hook đang cache với `periodId=undefined`. Scope thật trước query tránh stale/cross-period data. Helper cũ có thể vẫn phục vụ chat context/Phase 75.1; không đổi semantics nếu chưa audit call chain riêng. |
| 33 | 2026-08-26 | **Agy Sonnet 4.6 review gate cho Phase 96 + Phase 97 deferred**: R1 `PLAN_CHANGES_REQUIRED`, đã sửa; R2 `PLAN_PASS`, không còn Critical/Important finding | Xác nhận Active-only, `NO_ACTIVE_PERIOD`, `MULTIPLE_ACTIVE_PERIODS`, real `activePeriodId` cache scope, future history tree và closed-period read-only boundary |
| 34 | 2026-08-26 | **Phase 96 chuyển thành integrity-first trước UI performance**: server page wrapper + server-only Active resolver; live preflight; DB single-Active invariant; atomic period lifecycle; Closed firewall cho mọi evaluation write và SQL RPC; inline history chỉ closed-summary, Phase 97 vẫn là full history route deferred | Sequential Thinking + source audit phát hiện plan R2 cũ chỉ đóng read/UI contract, chưa ngăn multiple Active, partial create/delete, stale-tab writes hoặc RPC TOCTOU. Mọi migration/apply/deploy vẫn cần approval riêng. |
| 35 | 2026-08-26 | **Agy Sonnet 4.6 review gate cho bản Phase 96 integrity-first**: R3 `PLAN_CHANGES_REQUIRED`; đã bổ sung SQL patch mới cho RPC TOCTOU, closed-only history, standalone caller inventory, RSC topology và exact-empty lifecycle; R4 `PLAN_PASS` còn một Important đã sửa; R5 `PLAN_PASS` với Critical/Important/Non-blocking đều `NONE` | Chốt plan executable trước implementation nhưng không coi review là bằng chứng code đã sửa; P96T00 live preflight vẫn là bước đầu và production mutation vẫn gated. |
| 36 | 2026-08-26 | **P96T00 live preflight PASS**: live có đúng 1 Active, 53 evaluations, 69 rounds, 0 duplicate/orphan; repo migrations không có single-Active partial unique invariant | Agy `gemini-3.7-flash-high` thực thi read-only, Mika chạy aggregate độc lập; Agy `gemini-3.1-pro-high` adjudicate PASS. PostgREST chặn `pg_catalog`/`information_schema`, nên live catalog metadata UNKNOWN; không chặn read-only P96T01 nhưng P96T03 candidate/apply phải giữ direct-catalog gate. |
| 37 | 2026-08-26 | **P96T01 BLOCKED_AUTH**: local và production compare route đều redirect 307 → `/login`; không có authorized browser session nên cold/warm baseline 3 viewport = UNKNOWN, sample 0 | Agy `gemini-3.7-flash-high` không fabricated metric; Mika xác minh độc lập bằng `curl` local/production và worktree sạch. Không nhập credential/bypass auth; P96T02 giữ dependency gate. |
| 38 | 2026-08-26 | **P96T01 baseline PASS bằng Mika fallback**: sau khi bootstrap authorized Chrome profile/password trống, đo 3 cold + 3 warm mỗi viewport; median first-light→full 390 `3313/2685ms`, 768 `2898/2480ms`, 1440 `2994/2786ms` | Agy `gemini-3.7-flash-high` lần đầu `BLOCKED_AUTH`, lần retry timeout; Mika Playwright read-only hoàn tất. Route authenticated HTTP 200, resource count 25, CSP Report-Only warning duy nhất; không có app exception/failed request dai dẳng. P96T02 đủ dependency, chưa code change. |
| 39 | 2026-08-26 | **P96T02 PASS**: detail/compare chuyển sang RSC wrapper + server-only Active resolver; zero/multiple/error fail-closed; query/cache luôn có real `periodId`; employee read bỏ implicit Active fallback; inline history chỉ Closed Approved | Agy execution `gemini-3.7-flash-high` bị safety gate block 2 lần nên Mika direct fallback theo policy. Evidence: full suite 28/28, lint, tsc, build, authenticated localhost detail/compare 0 console/JS errors; fresh Agy `gemini-3.1-pro-high` review `PASS`, Critical/Important/Non-blocking `NONE`, HIGH. Chưa deploy/push. |
| 40 | 2026-08-26 | **P96T03 PASS_WITH_CONSTRAINT**: candidate partial unique index + preflight/rollback; close và save target require Active + exactly one affected row; create giữ DB uniqueness authority | Agy `gemini-3.7-flash-high` tạo đúng 4 file trong scope; Mika sửa timestamp migration, silent-index risk và overly broad 23505 mapping. Evidence: focused contract PASS, full suite 29/29, lint, tsc, build PASS; fresh Agy `gemini-3.1-pro-high` review PASS clean, HIGH. Live catalog `UNKNOWN/BLOCKED`: Management API và `supabase db query --linked` đều 403. Chưa apply/deploy/push; production approval + direct catalog privilege bắt buộc. |
| 41 | 2026-08-26 | **P96T04 PASS_WITH_CONSTRAINT**: period create/evaluations/round-1 chuyển sang atomic RPC với evaluator resolution giữ ở TypeScript; normal delete chỉ cho exact-empty Closed period, không hard-delete business data | Agy `gemini-3.7-flash-high` tạo candidate đúng 4 file; Mika sửa whitespace normalization trong contract test sau failure thật. Evidence: focused contract PASS, full suite 30/30, lint, tsc, build PASS; fresh Agy `gemini-3.1-pro-high` review PASS, Critical/Important/Non-blocking NONE, HIGH. Live DB failure injection/catalog `UNKNOWN/BLOCKED`; chưa apply/deploy/push. |
| 42 | 2026-08-26 | **P96T05 PASS_WITH_CONSTRAINT**: mọi direct evaluation/AI write caller có shared exact-Active fail-closed precheck; lazy initializer fail-closed zero/multiple Active; RPC wrapper khóa parent period `FOR UPDATE`, check exact `active`, delegate P3 trong cùng transaction | Agy `gemini-3.7-flash-high` tạo candidate đúng 8 file; Mika sửa lazy initializer để không chọn `limit(1)` khi multiple Active và thêm contract assertion. Evidence: focused contract PASS, full suite 31/31, lint, tsc, build PASS; fresh Agy `gemini-3.1-pro-high` review PASS, Critical/Important NONE, HIGH. Non-blocking: direct REST TOCTOU residual và lazy N+1 guard query. Live catalog/race/failure injection `UNKNOWN/BLOCKED`; chưa apply/deploy/push. |
| 43 | 2026-08-26 | **P96T06 PASS_WITH_CONSTRAINT**: compare route có reusable `CompareFrame`/`CompareHeader` cho loaded, loading, NO_ACTIVE, MULTIPLE_ACTIVE, missing-data và blocked states; loading static shell giữ geometry/markers, non-interactive, không fake data | Agy `gemini-3.7-flash-high` sửa reviewer finding R1; Mika verify focused + full suite 32/32 + lint/tsc/build; fresh `gemini-3.1-pro-high` reviewer R2 PASS, Critical/Important/Non-blocking NONE, HIGH. Authenticated browser `BLOCKED_AUTH` vì route 307 `/login`; chưa claim visual PASS, chưa deploy/push. |
| 44 | 2026-08-26 | **P96T07 PASS_WITH_CONSTRAINT**: vì compare chỉ có aggregate dependency, progressive behavior được giới hạn thành truthful DOM phase contract — primary summary/changed criteria trước secondary comments/unchanged; thêm `data-load-state=loading|ready`, không fake streaming/delay/retry/query | Agy `gemini-3.7-flash-high` tạo đúng 3 file; Mika verify focused + full suite 33/33 + lint/tsc/build; fresh `gemini-3.1-pro-high` review PASS, Critical/Important/Non-blocking NONE, HIGH. Browser authenticated `BLOCKED_AUTH` vì route 307 `/login`; chưa claim visual PASS, chưa deploy/push. |
